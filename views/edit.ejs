<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Dog</title>

            <!--BootStrap cdn-->
    <!-- CSS only -->
    <link rel="stylesheet" type="text/css" href="/styles/main.css" />

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">

</head>
<body>

    <%- include('partials/navbar') %>


      <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <h2 class="text-center mb-4">Edit a Dog</h2>
                <%# Using method-override to send a put request. html forms only allow to send post and get request. without method-override, we would've needed front end js to send the put and delete request %>

                <form action="/edit/<%= dog._id %>?_method=PUT" method="POST">
                    <div class="form-group">
                        <label for="name">Name</label>
                        <input class="form-control type="text" id="name" name="name" value="<%= dog.name %>" required>
                    </div>
                    <div class="form-group">
                        <label for="Age">Age</label>
                        <input class="form-control type="number" id="age" name="age" value="<%= dog.age %>" required>
                    </div>
                    <div class="form-group">
                        <label for="favoriteFood"> Faviorite Food</label>
                        <input class="form-control type="text" id="favoriteFood" name="favoriteFood" value="<%= dog.favoriteFood %>" required>
                    </div>
                    <div class="form-group">
                        <label for="funFact">Fun fact</label>
                        <input class="form-control type="text" id="funFact" name="funFact"  value="<%= dog.funFact %>" required>
                    </div>
                </div>
                <button type="submit" class="btn btn-primary">Update</button>
                </form>
            </div>
        </div>
      </div>
    
</body>
</html>