<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cat Gallery</title>

        <!--BootStrap cdn-->
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" 
    rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" 
    crossorigin="anonymous">

  </head>
  <body>
    <div>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/upload">Upload</a></li>
      </ul>
    </div>

    <% dogs.forEach(dog => { %>

        <div class="card" style="width: 18rem;">
            <img src="<%= dog.img %>" class="card-img-top" alt="...">
            <div class="card-body">
              <h5 class="card-title"><%= dog.name %></h5>
              <h5 class="card-title">Age: <%= dog.name %></h5>
              <h5 class="card-title">Favorite food: <%= dog.food %></h5>
              <h5 class="card-title">Fun fact: <%=
                dog.funFact %></h5>
        
              <a href="/edit/<%= dog._id %>" class="btn btn-primary">Edit</a>

              <%# Using method-override to send a delete request %>

              <form action="/delete/<%= dog._id %>"
                method="POST">
                <button class="btn btn-primary btnDelete" type="submit"><i class="bi bi-trash"></i></button>

            
              </form>
            </div>
          </div>
    <% }) %>
  </body>
</html>